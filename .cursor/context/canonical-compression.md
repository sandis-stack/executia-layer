# Phase 6A — Canonical compression

- **Module**: `executia-canonical-compression.js` + `.css` (governed-presentation bundle).
- **Enable**: `ex-compression-enabled` on engineering; `applyCompression(ctx)` replaces per-layer apply chain.
- **Core**: `CANONICAL` tokens + `META` one-liners · `displayLimit(5)` → 3.
- **Surfaces**: `postureBlocks` → single `canonicalPostureBlock` when active; no triple jurisdiction/objective/reliability blocks.
- **Bands**: tertiary meta hidden; frame rows hidden; tighter padding.
- **Core phrases**: shortened `PRESENCE`, `FRAME_INEVITABILITY`, authority lines.
- **Layers**: rhythm, consequence, memory, intent, trust, sovereignty read `isActive()` for short meta/secondary.
- **No** runtime/SQL/API/DB changes.
