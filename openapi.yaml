openapi: 3.0.0
info:
  title: EXECUTIA Final Full Layer API
  version: 8.0.0
paths:
  /api/v1/health:
    get:
      responses:
        '200':
          description: OK
  /api/v1/execute:
    get:
      responses:
        '200':
          description: List executions
    post:
      responses:
        '201':
          description: Create execution
  /api/v1/operator-decision:
    post:
      responses:
        '200':
          description: Apply operator decision
  /api/v1/ledger-verify:
    get:
      responses:
        '200':
          description: Verify ledger
